# CSCI580_Tabbatha_Motley

All files for Assignment are in folder. Source code uses Colab. Either click run all button at the top of the window or highlight the first cell and press "Shift+Enter" for each cell. To run grade checker function run the function definition cell first then its output cell above it, this will clear any "undefined function" errors. AI collaboration log is also provided in assignment folder with disclosure of tools useage at the bottom of the report.
